// Public browser configuration only. Never put an API key in this file.
window.PORTFOLIO_CONFIG = {
  // Leave blank while the API is served from the same origin during local development.
  // For GitHub Pages, set this to the public URL of the server-side chat proxy.
  // Example: "https://portfolio-api.neyobytes.com/api/chat"
  chatEndpoint: ''
};